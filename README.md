# Simple File Parser (Python) #

This simple Python script will ask for an input file to search/parse. 
Next, a search string will need to be provided and the script will ask to save the results to an output file (CSV/TXT). 

